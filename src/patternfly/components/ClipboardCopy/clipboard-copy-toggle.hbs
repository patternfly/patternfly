<button class="pf-c-clipboard-copy__toggle{{#if clipboard-copy-toggle--modifier}} {{clipboard-copy-toggle--modifier}}{{/if}}"
  {{#if clipboard-copy-toggle--attribute}}
    {{{clipboard-copy-toggle--attribute}}}
  {{/if}}>
  <i class="fas fa-angle-right"></i>
  {{> @partial-block}}
</button>
