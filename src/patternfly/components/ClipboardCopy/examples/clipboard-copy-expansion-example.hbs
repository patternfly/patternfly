{{#> clipboard-copy}}
	{{#> input-group}}
    {{#> clipboard-copy-group-toggle clipboard-copy-group-toggle--attribute='aria-expanded="false" aria-controls="ex-content-1"'}}{{/clipboard-copy-group-toggle}}
    {{#> form-control controlType="input" input="true" form-control--attribute='type="text" value="Got a lot of text here, need to see all of it? Click that arrow on the left side and check out the resulting expansion." id="textInput3" aria-label="standard input"'}}
    {{/form-control}}
	  {{#> clipboard-copy-group-copy}}{{/clipboard-copy-group-copy}}
	{{/input-group}}
  {{#> clipboard-copy-expanded-content clipboard-copy-expanded-content--attribute='id="ex-content-1"'}}
    Got a lot of text here, need to see all of it? Click that arrow on the left side and check out the resulting expansion.
  {{/clipboard-copy-expanded-content}}
{{/clipboard-copy}}
<br>
<br>
{{#> clipboard-copy clipboard-copy--IsExpanded="true"}}
	{{#> input-group}}
    {{#> clipboard-copy-group-toggle clipboard-copy-group-toggle--attribute='aria-expanded="false" aria-controls="ex-content-2"'}}{{/clipboard-copy-group-toggle}}
    {{#> form-control controlType="input" input="true" form-control--attribute='type="text" value="Got a lot of text here, need to see all of it? Click that arrow on the left side and check out the resulting expansion." id="textInput4" aria-label="standard input"'}}
    {{/form-control}}
	  {{#> clipboard-copy-group-copy}}{{/clipboard-copy-group-copy}}
	{{/input-group}}
  {{#> clipboard-copy-expanded-content clipboard-copy-expanded-content--attribute='id="ex-content-2"'}}
    Got a lot of text here, need to see all of it? Click that arrow on the left side and check out the resulting expansion.
  {{/clipboard-copy-expanded-content}}
{{/clipboard-copy}}
