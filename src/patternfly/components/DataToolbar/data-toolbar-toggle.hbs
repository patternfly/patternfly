<div class="pf-c-data-toolbar__toggle{{#if data-toolbar-toggle--modifier}} {{data-toolbar-toggle--modifier}}{{/if}}"
  {{#if data-toolbar-toggle--attribute}}
    {{{data-toolbar-toggle--attribute}}}
  {{/if}}>
  {{#> button button--modifier="pf-m-plain" button--attribute=(concat 'aria-label="Show filters" aria-expanded="' data-toolbar-toggle--IsExpanded '" aria-controls="' data-toolbar--id '-expandable-content"')}}
    <i class="fas fa-filter" aria-hidden="true"></i>
  {{/button}}
</div>
