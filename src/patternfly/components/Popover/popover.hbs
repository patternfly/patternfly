<div class="pf-c-popover{{#if popover--IsDangerAlert}} pf-m-danger{{else if popover--IsWarningAlert}} pf-m-warning{{else if popover--IsSuccessAlert}} pf-m-success{{else if popover--IsInfoAlert}} pf-m-info{{else if popover--IsDefaultAlert}} pf-m-default{{/if}}{{#if popover--modifier}} {{popover--modifier}}{{/if}}"
  aria-modal="true"
  {{#if popover--attribute}}
    {{{popover--attribute}}}
  {{/if}}>
  {{#> popover-arrow}}
  {{/popover-arrow}}
  {{> @partial-block}}
</div>
