<div class="pf-c-popover{{#if popover--modifier}} {{popover--modifier}}{{/if}}"
  {{#if popover--attribute}}
    {{popover--attribute}} 
  {{/if}}>
  {{#> popover-arrow}}
  {{/popover-arrow}}
  {{> @partial-block}}
</div>
