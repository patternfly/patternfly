<header class="{{pfv}}popover__header{{#if popover-header--modifier}} {{popover-header--modifier}}{{/if}}"
  {{#if popover-header--attribute}}
    {{{popover-header--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</header>
