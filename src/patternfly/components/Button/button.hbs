<button class="pf-c-button{{#if button--modifier}} {{button--modifier}}{{/if}}"
  {{#if button--IsSubmit}} 
    type="submit"
  {{else if button--IsReset}}
    type="reset"
  {{else}}
    type="button"
  {{/if}}
  {{#if button--attribute}}
    {{{button--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</button>
