<button class="pf-c-button {{ btnClass }}" 
  {{#if btnAttributes}}
    {{{ btnAttributes }}} 
  {{/if}}
  {{#if aria-label}}
    aria-label="{{aria-label}}"
  {{/if}}
  >

  {{#if @partial-block}}
    {{> @partial-block }}
  {{/if}}
  
</button>
