<aside class="pf-c-drawer__panel{{#if drawer-panel--modifier}} {{drawer-panel--modifier}}{{/if}}"
  {{#unless drawer-panel--IsOpen}}
    aria-hidden="true"
  {{/unless}}
  {{#if drawer-panel--attribute}}
    {{{drawer-panel--attribute}}}
  {{/if}}>
  {{#> drawer-panel-body}}
    {{> @partial-block}}
  {{/drawer-panel-body}}
</aside>
