<{{#if tabs--type}}{{tabs--type}}{{else}}div{{/if}} class="pf-c-tabs
  {{~#if tabs--IsSecondary}} pf-m-secondary{{/if}}
  {{~#if tabs--IsVertical}} pf-m-vertical{{/if}}
  {{~#if tabs--IsExpandable}} pf-m-expandable{{/if}}
  {{~#if tabs--IsNonExpandable}} pf-m-non-expandable{{/if}}
  {{~#if tabs--IsExpanded}} pf-m-expanded{{/if}}
  {{~#if __tabs-list--IsOverflow}} pf-m-overflow{{/if}}
  {{~#if tabs--modifier}} {{tabs--modifier}}{{/if}}"
  {{#if tabs--attribute}}
    {{{tabs--attribute}}}
  {{/if}}
  {{#if tabs--id}}
    id="{{{tabs--id}}}"
  {{/if}}>
  {{> @partial-block}}
</{{#if tabs--type}}{{tabs--type}}{{else}}div{{/if}}>
