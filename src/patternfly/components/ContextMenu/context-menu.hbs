<ul class="pf-c-context-menu{{#if context-menu--modifier}} {{context-menu--modifier}}{{/if}}"
  {{#if context-menu--attribute}}
    {{{context-menu--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</ul>
