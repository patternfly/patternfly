<button class="pf-c-dropdown__toggle-action{{#if dropdown-toggle-action--modifier}} {{dropdown-toggle-action--modifier}}{{/if}}"
  {{#if aria-label}}
    aria-label="{{aria-label}}"
  {{/if}}
  {{#if dropdown-toggle--IsDisabled}}
    disabled
  {{/if}}
  {{#if dropdown-toggle-action--attribute}}
	  {{{dropdown-toggle-action--attribute}}}
  {{/if}}>
  {{{@partial-block}}}
</button>
