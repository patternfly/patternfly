<button class="pf-c-dropdown__toggle-button{{#if dropdown-toggle-button--modifier}} {{dropdown-toggle-button--modifier}}{{/if}}"
  type="button"
  {{#if dropdown--IsToggleButton}}
    aria-expanded="{{#if dropdown--IsExpanded}}true{{else}}false{{/if}}"
    id="{{dropdown--id}}-toggle-button"
  {{/if}}
  aria-label="{{#if dropdown-toggle-button--aria-label}}
    {{dropdown-toggle-button--aria-label}}
  {{else}}
    dropdown toggle
  {{/if}}"
  {{#if dropdown-toggle--IsDisabled}}
    disabled
  {{/if}}
  {{#if dropdown-toggle-button--attribute}}
    {{{dropdown-toggle-button--attribute}}}
  {{/if}}>
  {{#if @partial-block}}
    {{> @partial-block}}
  {{else}}
    <i class="fas fa-caret-down" aria-hidden="true"></i>
  {{/if}}
</button>
