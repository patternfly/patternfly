<button class="{{pfv}}options-menu__toggle-button{{#if options-menu-toggle-button--modifier}} {{options-menu-toggle-button--modifier}}{{/if}}"
  id="{{options-menu--id}}-toggle"
  aria-haspopup="listbox"
  aria-expanded="{{#if options-menu--IsExpanded}}true{{else}}false{{/if}}"
  {{#if options-menu-toggle-button--aria-label}}
    aria-label="{{options-menu-toggle-button--aria-label}}"
  {{/if}}
  {{#if options-menu-toggle--IsDisabled}}
    disabled
  {{/if}}
  {{#if options-menu-toggle-button--attribute}}
    {{{options-menu-toggle-button--attribute}}}
  {{/if}}>
  {{> options-menu-toggle-button-icon}}
</button>