<li class="{{pfv}}wizard__nav-item
  {{~#if wizard-nav-item--IsExpandable}}
    pf-m-expandable
  {{~/if~}}
  {{~#if wizard-nav-item--IsExpanded}}
    pf-m-expanded
  {{~/if~}}
  {{~#if wizard-nav-item--modifier}} {{wizard-nav-item--modifier}}{{/if}}"
  {{#if wizard-nav-item--attribute}}
    {{{wizard-nav-item--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</li>
