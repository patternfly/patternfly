<div class="pf-c-form__select {{ selectStyleModifier }}">
  <select id="{{ selectId }}" name="{{ selectName }}" {{#if disabled}} disabled {{/if}} {{#if multiple}} multiple="true" {{/if}}>
    {{> @partial-block }}
  </select>
</div>
