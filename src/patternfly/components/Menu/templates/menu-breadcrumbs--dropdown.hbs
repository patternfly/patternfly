{{#> breadcrumb-item}}
  {{> breadcrumb-item-divider}}
  {{#> breadcrumb-dropdown}}
    {{#> dropdown dropdown--id=(concat breadcrumb--id '-breadcrumb-dropdown')}}
      {{> dropdown-toggle dropdown-toggle--IsBadge="true"}}
      {{#> dropdown-menu}}
        {{> @partial-block}}
      {{/dropdown-menu}}
    {{/dropdown}}
  {{/breadcrumb-dropdown}}
{{/breadcrumb-item}}
