<textarea class="{{pfv}}label-group__textarea{{#if label-group-textarea--modifier}} {{label-group-textarea--modifier}}{{/if}}"
  rows="1"
  tabindex="0"
  {{#if label-group-textarea--aria-label}}
    aria-label="{{label-group-textarea--aria-label}}"
  {{else}}
    aria-label="New label"
  {{/if}}
  {{#if label-group-textarea--attribute}}
    {{{label-group-textarea--attribute}}}
  {{/if}}>
</textarea>
