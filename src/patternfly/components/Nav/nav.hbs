<nav class="pf-c-nav{{#if nav--modifier}} {{nav--modifier}}{{/if}}"
  {{#if nav--IsOverflowLeft}}
    pf-m-overflow-left
  {{/if}}
  {{#if aria-label}}
    aria-label="{{aria-label}}"
  {{else}}
    aria-label="Global"
  {{/if}}
  {{#if nav--attribute}}
    {{{nav--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</nav>
