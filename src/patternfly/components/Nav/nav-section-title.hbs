{{setTag nav-section-title--type nav-section-title--type 'h2'}}

<{{tag}} class="{{pfv}}nav__section-title{{#if nav-section-title--modifier}} {{nav-section-title--modifier}}{{/if}}"
  {{#if nav-section-title--id}}
    id="{{nav-section-title--id}}"
  {{/if}}
  {{#if nav-section-title--attribute}}
    {{{nav-section-title--attribute}}}
  {{/if}}>
  {{#if nav-section-title--text}}
    {{{nav-section-title--text}}}
  {{else if @partial-block}}
    {{> @partial-block}}
  {{/if}}
</{{tag}}>
