<{{#if masthead--type}}{{masthead--type}}{{else}}header{{/if}} class="{{pfv}}masthead
  {{setModifiers
    masthead--IsDocked='pf-m-docked'
    masthead--modifier=masthead--modifier
  }}"
  {{#if masthead--id}}
    id="{{masthead--id}}"
  {{/if}}
  {{#if masthead--attribute}}
    {{{masthead--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</{{#if masthead--type}}{{masthead--type}}{{else}}header{{/if}}>
