<header class="pf-c-masthead{{#if masthead--modifier}} {{masthead--modifier}}{{/if}}"
  {{#if masthead--attribute}}
    {{masthead--attribute}}
  {{/if}}
  {{#if masthead--id}}
    id="{{{masthead--id}}}"
  {{/if}}>
  {{> @partial-block}}
</header>
