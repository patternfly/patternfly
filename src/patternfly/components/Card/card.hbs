<div class="{{pfv}}card{{#if card--modifier}} {{card--modifier}}{{/if}}"
  {{#if card--attribute}}
    {{{card--attribute}}}
  {{/if}}
  {{#if card--id}}
    id="{{{card--id}}}"
  {{/if}}>
  {{> @partial-block}}
</div>
