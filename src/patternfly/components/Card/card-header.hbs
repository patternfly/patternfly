<div class="{{pfv}}card__header{{#if card-header--modifier}} {{card-header--modifier}}{{/if}}"
  {{#if card-header--attribute}}
    {{{card-header--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</div>
