<header class="{{pfv}}modal-box__header{{#if modal-box-header--modifier}} {{modal-box-header--modifier}}{{/if}}"
  {{#if modal-box-header--attribute}}
    {{{modal-box-header--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</header>
