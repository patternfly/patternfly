<button id="{{id}}-button" class="pf-c-app-launcher__toggle{{#if app-launcher-toggle--modifier}} {{app-launcher-toggle--modifier}}{{/if}}"
  aria-expanded="{{#if app-launcher--IsExpanded}}true{{else}}false{{/if}}"
  {{#if aria-label}} aria-label="{{aria-label}}"{{/if}}
  {{#if app-launcher-toggle--attribute}}
    {{{app-launcher-toggle--attribute}}}
  {{/if}}>
    {{> app-launcher-toggle-icon}}
</button>
