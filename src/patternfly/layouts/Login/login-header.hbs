<header class="pf-l-login__header {{#if login-header--modifier}} {{login-header--modifier}}{{/if}}"
  {{#if login-header--attribute}}
    {{{login-header--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</header>